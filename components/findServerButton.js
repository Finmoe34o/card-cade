"use client"

export default function Button() {
    const findServer = () => {
        let pNum = 7
        let href
        while (playerNum > 6) {
            if (servers[0]) {
                pNum = 2
            }
        }
    }
    return <button onClick={findServer} className="w-[15vw] h-[5vh] relative top-[38vh] bg-gray-600 rounded-2xl mx-auto">Find a table</button>
}